---
title: AI群聊发现器 - 开发日志
description: MVP 开发进度追踪,用于 vibe coding 时的上下文恢复
version: 1.0
date: 2026-04-02
status: 进行中
product_docs: 产品总纲.md | PRD.md | ARCHITECTURE.md | DESIGN.md | README.md
---

> ⚠️ **大模型必读**:开始开发前,请先通读本文档了解当前进度。开发过程中每完成一个模块并在本地测试稳定后,**立即在本文件对应条目下记录完成日期和测试结论**。不要跳过此步骤--这是唯一的事实来源。

---

# 一、项目概述

| 属性 | 内容 |
|------|------|
| 产品名称 | AI群聊发现器 |
| 核心功能 | 帮助用户快速找到 AI 产品官方群聊(微信/QQ/飞书)入口的内部调研工具 |
| 技术栈 | React + Vite + TypeScript(前端) / FastAPI + SQLite(后端) |
| MVP 定位 | 本地单用户,无需登录,每次搜索实时抓取 |
| 相关文档 | [[产品总纲]] · [[PRD]] · [[ARCHITECTURE]] · [[DESIGN]] · [[03 Projects/AI群聊发现器/README]] |

---

# 二、MVP 开发 TODO LIST

> 严格按 Phase 顺序开发。前置依赖未完成时,不得跳过进入下一 Phase。
> 每项完成后:在 `[ ]` 中填入 `[x]`,并在后面标注 `✅ 完成于 YYYY-MM-DD | 测试结论:xxx`
> 测试结论必须说明:功能是否正常运行、已知问题、是否可合并到主分支。

---

## 🔧 Phase 1:项目脚手架与环境搭建

**目标**:建立前后端基础工程,跑通本地运行闭环

| # | 任务 | 依赖 | 状态 | 完成日期 | 测试结论 |
|---|------|------|------|---------|---------|
| 1.1 | 创建 React + Vite + TypeScript 前端项目,配置 ESLint + Prettier | 无 | [x] | 2026-04-01 | `npm install`、`npm run format:check`、`npm run lint`、`npm run build` 通过;前端占位页可渲染;可合并到主分支 |
| 1.2 | 创建 FastAPI 后端项目,配置 uvicorn 热重载 | 无 | [x] | 2026-04-01 | FastAPI 工程已建立,`uvicorn main:app --host 127.0.0.1 --port 8000` 可启动并返回健康检查;可合并到主分支 |
| 1.3 | 配置 Playwright,安装 Chromium 浏览器运行时 | 无 | [x] | 2026-04-01 | `python -m playwright install chromium` 完成,`scripts/check_playwright.py` 返回 `playwright-ok`;可合并到主分支 |
| 1.4 | 配置 SQLite 数据库,创建 `added_groups` / `search_history` / `crawl_snapshot` 三张表 | 无 | [x] | 2026-04-01 | 后端启动时自动初始化数据库文件,三张业务表已创建;可合并到主分支 |
| 1.5 | 安装所有后端依赖:`fastapi`, `httpx`, `beautifulsoup4`, `playwright`, `opencv-python` | 1.2, 1.3 | [x] | 2026-04-01 | `.venv` 已创建,`pip install -r requirements.txt` 完成,`python -m compileall .\\app .\\main.py .\\scripts` 通过;可合并到主分支 |
| 1.6 | 配置前后端联调(CORS、本地 API 代理),跑通"Hello World"接口 | 1.1, 1.2 | [x] | 2026-04-01 | 已配置 FastAPI CORS 与 Vite `/api` 代理,前端通过代理成功访问 `GET /api/health`;可合并到主分支 |
| 1.7 | 配置 Git 仓库,创建 `.gitignore`(排除 `.venv`、`__pycache__`、`node_modules`) | 1.1, 1.2 | [x] | 2026-04-01 | 根目录 Git 仓库初始化完成,`.gitignore` 已覆盖前后端依赖与构建产物;可合并到主分支 |
| 1.8 | 编写 `README.md` 的"怎么运行"章节(将技术栈选型写入实际选型) | 1.1~1.7 | [x] | 2026-04-01 | 根级 `README.md` 已对齐 `app/frontend`、`app/backend` 实际目录与启动步骤;可合并到主分支 |

---

## 🔧 Phase 2:后端核心模块开发

**目标**:完成搜索流程 8 步的数据层和逻辑层,API 接口就绪

| # | 任务 | 依赖 | 状态 | 完成日期 | 测试结论 |
|---|------|------|------|---------|---------|
| 2.1 | `SearchEntry` 模块:接收产品名/别名/域名,标准化查询词 | Phase 1 | [x] | 2026-04-02 | `SearchEntryTests` 覆盖产品名与域名标准化,`POST /api/search` 已接入统一标准化入口;可合并到主分支 |
| 2.2 | `CandidateResolver` 模块:处理产品名歧义,返回最多 3 个候选产品 | 2.1 | [ ] | - | - |
| 2.3 | `PageFetcher` 模块:并行抓取官网 + GitHub 页面(httpx 静态 + Playwright 动态) | Phase 1 | [ ] | - | - |
| 2.4 | `OfficialSourceValidator` 模块:校验页面属于官方域名和官方主体 | 2.3 | [x] | 2026-04-02 | `OfficialSourceValidatorTests` 覆盖官网同域与官方 GitHub 仓库校验,第三方主体链接会被过滤;可合并到主分支 |
| 2.5 | `EntryExtractor` 模块:提取二维码图片(img 标签)或入群链接(weixin:// / feishu:// 等) | 2.4 | [x] | 2026-04-02 | `EntryExtractorTests` 覆盖二维码图片/入群链接提取,二维码包裹链接去重逻辑已接入搜索链路;可合并到主分支 |
| 2.6 | `QRCodeDetector`:用 OpenCV 识别下载的二维码图片是否可用 | 2.5 | [x] | 2026-04-02 | OpenCV 二维码识别与静态资源落盘已接入 `EntryExtractor`,相关单测通过;可合并到主分支 |
| 2.7 | `GroupTypeClassifier` 模块:依据周边文案关键词识别 5 种群类型(交流/答疑/售后/招募-内测/未知) | 2.5 | [x] | 2026-04-02 | `GroupTypeClassifierTests` 通过,已支持交流/答疑/售后/招募-内测/未知五类枚举;可合并到主分支 |
| 2.8 | `ResultNormalizer` 模块:归一化输出 `ProductCard[]` 数据结构 | 2.6, 2.7 | [x] | 2026-04-02 | `ResultNormalizerTests` 覆盖正常路径和空值路径,`ProductCard[]` 已接入 `/api/search` 响应;可合并到主分支 |
| 2.9 | `GitHub API` 集成:抓取官方仓库的 stars 和创建时间 | 2.3 | [x] | 2026-04-02 | GitHub 仓库 stars / 创建时间 / description 拉取已接入,`SearchServiceMetadataTests` 覆盖元数据解析;可合并到主分支 |
| 2.10 | `LocalStateManager` 模块:读写 SQLite,已添加状态按群粒度保存 | Phase 1.4 | [ ] | - | - |
| 2.11 | 弱更新机制:每次搜索实时抓取,不依赖缓存,刷新 `verifiedAt` | 2.8, 2.10 | [ ] | - | - |
| 2.12 | **后端 API 接口**(FastAPI Router):`POST /search`、`POST /groups/:id/add`、`GET /groups/hidden`、`DELETE /groups/:id/add` | 2.1~2.11 | [ ] | - | - |
| 2.13 | 后端单元测试:每个模块至少一个测试用例,覆盖正常路径和空值路径 | 2.1~2.12 | [ ] | - | - |
| 2.S1 | 搜索可靠性第一轮稳定化(P0 + P1):补齐可观测性、官方目标发现精度、内链/图片/QQ 兼容修补 | 2.1, 2.3~2.9 | [x] | 2026-04-02 | 已新增 `search_with_trace()`、结构化 debug trace、官方网页候选打分、GitHub candidate 置信度收紧、`collect_relevant_internal_links()` 扩容、Playwright 短等待回退、QQ / 懒加载图片兼容;24 项后端单测通过,真实查询可区分"正确空结果 / 提取失败 / 无可靠官方目标";可合并到主分支 |
| 2.S2 | GitHub 完整 URL 输入修复：输入 `github.com/{owner}/{repo}` 时不再被错误解析为 `github.com` 域名 | 2.1 | [x] | 2026-04-02 | `GITHUB_URL_PATTERN` 正则优先匹配，`query_type=github_repo` 独立分支，`explicit_repo_url` 直接作为 `github_repo_url`；`GitHubRepositoryMetadata` 新增 `homepage` 字段从 GitHub API 补充官网 URL；代码审查通过，可合并到主分支 |
| 2.S3 | QR 解码降级 + 搜索扩量：`extract_image_candidate` QR 失败后用 URL 平台关键词降级；`<details>` 展开；多语言搜索词变体 | 2.1, 2.3~2.9 | [x] | 2026-04-02 | QR 降级：URL 含 `feishu.cn` 等平台关键词时不丢弃；`<details>` 展开：Playwright 主动展开折叠区；搜索变体：`{query} 官方群` / `{query} 官网` / `{query} github`；代码审查通过，可合并到主分支 |

---

## 🔧 Phase 3:前端核心组件开发

**目标**:完成所有 UI 组件和交互逻辑,与后端 API 对接

| # | 任务 | 依赖 | 状态 | 完成日期 | 测试结论 |
|---|------|------|------|---------|---------|
| 3.1 | `SearchBox` 组件:输入框 + Enter 触发 + 清空按钮 + 候选联想 debounce 300ms(仅联想,不直接抓取) | Phase 1 | [ ] | - | - |
| 3.2 | `CandidateDropdown` 组件:歧义时展示最多 3 个候选,点击后发起正式搜索 | 3.1, 2.2 | [ ] | - | - |
| 3.3 | `FilterBar` 组件:平台筛选(微信/QQ/飞书)、群类型筛选(5种)、排序按钮(热门/最新)、隐藏已添加开关 | 3.1 | [ ] | - | - |
| 3.4 | `ProductCard` 组件(折叠态):展示 7 个字段 + 入群二维码缩略图 + 群类型标签 + 已添加按钮 + 展开按钮 | 3.1 | [ ] | - | - |
| 3.5 | `ProductCard` 组件(展开态):展示 `groups[]` 全量列表,每条独立子群 + 独立已添加按钮 | 3.4 | [ ] | - | - |
| 3.6 | `QRModal` 组件:二维码放大查看 Modal + 入群按钮(有二维码→Modal / 无二维码→直接打开链接) | 3.4 | [ ] | - | - |
| 3.7 | `EmptyState` 组件:统一空结果态(文案:"未发现该产品的官方群"),不展示替代入口 | 3.1 | [x] | 2026-04-02 | 空结果态已统一显示"未发现该产品的官方群",`POST /api/search` 烟测返回该文案,`npm run build` 通过;可合并到主分支 |
| 3.8 | `SkeletonCard` 组件:骨架屏占位,与主卡片结构一致,原位替换 | 3.1 | [ ] | - | - |
| 3.9 | 页面布局串联:搜索框 → 筛选栏 → 结果区,固定三段式结构 | 3.1~3.8 | [ ] | - | - |
| 3.10 | 前端状态管理:搜索结果列表 + 筛选条件 + 已添加列表,与后端 API 对接 | 3.1~3.9, 2.12 | [ ] | - | - |
| 3.11 | 空值处理前端适配:stars 空→"-" / 创建时间空→"未知" / 入群方式无二维码→标注"二维码暂未抓取成功" | 3.4, 3.6 | [ ] | - | - |
| 3.12 | 前端单元测试:组件渲染测试 + 交互测试(搜索、筛选、添加、展开) | 3.1~3.11 | [ ] | - | - |

---

## 🔧 Phase 4:集成测试与稳定化

**目标**:前后端联调通过,覆盖主要用户路径,消除已知问题

| # | 任务 | 依赖 | 状态 | 完成日期 | 测试结论 |
|---|------|------|------|---------|---------|
| 4.1 | 端到端搜索测试(以 Cursor / Claude / Lovable 为例):搜索 → 候选选择 → 结果卡片 → 展开 → 入群 | Phase 2, 3 | [ ] | - | - |
| 4.2 | 筛选功能测试:平台筛选 + 群类型筛选 + 热门排序 + 最新排序,全路径通过 | 4.1 | [ ] | - | - |
| 4.3 | 已添加功能测试:标记已添加 → 默认隐藏 → 关闭开关 → 弱化显示 → 取消隐藏 | 4.1 | [ ] | - | - |
| 4.4 | 空结果态测试:搜索无官方群的产品 → 明确提示"未发现该产品的官方群",不展示替代入口 | 4.1 | [ ] | - | - |
| 4.5 | 二维码无结果测试:搜索到无二维码的产品 → 展示入口链接 + 标注"二维码暂未抓取成功" | 4.1 | [ ] | - | - |
| 4.6 | 候选产品测试:搜索别名歧义产品 → 返回 3 个候选 → 用户选择 → 正式搜索 | 4.1 | [ ] | - | - |
| 4.7 | 弱更新验证:同一产品搜索两次 → `verifiedAt` 时间戳更新 | 4.1 | [ ] | - | - |
| 4.8 | 本地状态持久化验证:标记已添加 → 关闭浏览器 → 重新打开 → 已添加状态仍保留 | 4.3 | [ ] | - | - |
| 4.9 | 异常处理验证:抓取超时 / GitHub API 限流 → 正确降级,不崩溃 | Phase 2 | [ ] | - | - |
| 4.10 | 性能验证:单次搜索响应时间 < 10s(网络正常情况下) | 4.1 | [ ] | - | - |
| 4.11 | 已知问题修复记录:将 Phase 1~4 中发现的问题逐一记录到 § 四 | 全阶段 | [ ] | - | - |

---

## 🔧 Phase 5:文档完善与发布准备

**目标**:输出完整文档,Review 通过后发布 MVP

| # | 任务 | 依赖 | 状态 | 完成日期 | 测试结论 |
|---|------|------|------|---------|---------|
| 5.1 | 更新 README.md:将实际运行步骤、技术栈版本、环境要求写入 | Phase 1~4 | [ ] | - | - |
| 5.2 | 更新 ARCHITECTURE.md:将实际模块拆分、技术选型、数据结构写入最终版本 | Phase 2 | [ ] | - | - |
| 5.3 | 更新 DESIGN.md:将实际组件实现、状态设计、空态处理写入最终版本 | Phase 3 | [ ] | - | - |
| 5.4 | 更新 PRD.md:将实际实现的功能范围、边界处理、异常处理写入最终版本 | Phase 2~4 | [ ] | - | - |
| 5.5 | Reviewer Agent 审核:提交四份文档 → Reviewer 检查 8 项一致性 → 修订清单 | 5.1~5.4 | [ ] | - | - |
| 5.6 | 修订问题修复:根据修订清单修复所有 🔴 阻断问题 | 5.5 | [ ] | - | - |
| 5.7 | MVP 版本发布:确认所有 Phase 1~4 任务 ✅ → 合并主分支 → 记录发布版本号 | 5.6 | [ ] | - | - |

---

# 三、已完成记录

> 格式:`✅ [任务编号] [任务名称] | 完成于 YYYY-MM-DD | 测试结论:xxx`

✅ [1.1] 创建 React + Vite + TypeScript 前端项目,配置 ESLint + Prettier | 完成于 2026-04-01 | 测试结论:`npm install`、`npm run format:check`、`npm run lint`、`npm run build` 全部通过,页面骨架可渲染,可合并到主分支

✅ [1.2] 创建 FastAPI 后端项目,配置 uvicorn 热重载 | 完成于 2026-04-01 | 测试结论:`uvicorn main:app --host 127.0.0.1 --port 8000` 可启动,`GET /api/health` 返回正常,可合并到主分支

✅ [1.3] 配置 Playwright,安装 Chromium 浏览器运行时 | 完成于 2026-04-01 | 测试结论:Chromium 安装完成,`scripts/check_playwright.py` 烟测通过,可合并到主分支

✅ [1.4] 配置 SQLite 数据库,创建 `added_groups` / `search_history` / `crawl_snapshot` 三张表 | 完成于 2026-04-01 | 测试结论:数据库文件已生成,三张业务表创建完成,可合并到主分支

✅ [1.5] 安装所有后端依赖:`fastapi`, `httpx`, `beautifulsoup4`, `playwright`, `opencv-python` | 完成于 2026-04-01 | 测试结论:虚拟环境与依赖安装完成,后端源码编译检查通过,可合并到主分支

✅ [1.6] 配置前后端联调(CORS、本地 API 代理),跑通"Hello World"接口 | 完成于 2026-04-01 | 测试结论:前端 dev server 可通过 `/api` 代理访问后端健康检查接口,联调闭环成立,可合并到主分支

✅ [1.7] 配置 Git 仓库,创建 `.gitignore`(排除 `.venv`、`__pycache__`、`node_modules`) | 完成于 2026-04-01 | 测试结论:Git 仓库已初始化,忽略规则生效,可合并到主分支

✅ [1.8] 编写 `README.md` 的"怎么运行"章节(将技术栈选型写入实际选型) | 完成于 2026-04-01 | 测试结论:根级 `README.md` 已与 `app/frontend`、`app/backend` 实际结构对齐,可合并到主分支

✅ [2.1] `SearchEntry` 模块:接收产品名/别名/域名,标准化查询词 | 完成于 2026-04-02 | 测试结论:`SearchEntryTests` 覆盖产品名与域名标准化,`POST /api/search` 已接入统一标准化入口,可合并到主分支

✅ [2.4] `OfficialSourceValidator` 模块:校验页面属于官方域名和官方主体 | 完成于 2026-04-02 | 测试结论:`OfficialSourceValidatorTests` 覆盖官网同域与官方 GitHub 仓库校验,第三方主体链接会被过滤,可合并到主分支

✅ [2.5] `EntryExtractor` 模块:提取二维码图片(img 标签)或入群链接(weixin:// / feishu:// 等) | 完成于 2026-04-02 | 测试结论:`EntryExtractorTests` 覆盖二维码图片/入群链接提取,二维码包裹链接去重逻辑已接入搜索链路,可合并到主分支

✅ [2.6] `QRCodeDetector`:用 OpenCV 识别下载的二维码图片是否可用 | 完成于 2026-04-02 | 测试结论:OpenCV 二维码识别与静态资源落盘已接入 `EntryExtractor`,相关单测通过,可合并到主分支

✅ [2.7] `GroupTypeClassifier` 模块:依据周边文案关键词识别 5 种群类型(交流/答疑/售后/招募-内测/未知) | 完成于 2026-04-02 | 测试结论:`GroupTypeClassifierTests` 通过,已支持交流/答疑/售后/招募-内测/未知五类枚举,可合并到主分支

✅ [2.8] `ResultNormalizer` 模块:归一化输出 `ProductCard[]` 数据结构 | 完成于 2026-04-02 | 测试结论:`ResultNormalizerTests` 覆盖正常路径和空值路径,`ProductCard[]` 已接入 `/api/search` 响应,可合并到主分支

✅ [2.9] `GitHub API` 集成:抓取官方仓库的 stars 和创建时间 | 完成于 2026-04-02 | 测试结论:GitHub 仓库 stars / 创建时间 / description 拉取已接入,`SearchServiceMetadataTests` 覆盖元数据解析,可合并到主分支

✅ [3.7] `EmptyState` 组件:统一空结果态(文案:"未发现该产品的官方群"),不展示替代入口 | 完成于 2026-04-02 | 测试结论:空结果态已统一显示"未发现该产品的官方群",`POST /api/search` 烟测返回该文案,`npm run build` 通过,可合并到主分支

✅ [2.S3] QR 解码降级 + 搜索扩量：`_extract_image_candidate` 增加 QR 解码失败后的平台 URL 降级检测（URL 含 `feishu.cn` 等关键词时不丢弃）；`_fetch_with_playwright` 增加 `<details>` 展开（使折叠区 QR 码进入 DOM）；`_search_multi_variants` 增加多语言查询变体（`{query} 官方群` / `{query} 官网` / `{query} github`），合并去重后最多 15 条；代码审查通过，可合并到主分支

✅ [2.S2] GitHub 完整 URL 输入修复:将 `github.com/{owner}/{repo}` 从"被错误解析为域名"改为直接作为 `github_repo_url` 处理 | 完成于 2026-04-02 | 测试结论:输入 `https://github.com/labring/FastGPT` 不再被解析为 `github.com` 首页;`NormalizedQuery.query_type` 新增 `github_repo` 类型,`_discover_targets` 新增 `explicit_repo_url` 分支直接路由;`GitHubRepositoryMetadata` 新增 `homepage` 字段用于从 GitHub API 发现官网 URL;代码审查通过,逻辑与产品总纲一致,可合并到主分支

✅ [2.S1] 搜索可靠性第一轮稳定化(P0 + P1):补齐可观测性、官方目标发现精度、内链/图片/QQ 兼容修补 | 完成于 2026-04-02 | 测试结论:新增 `search_with_trace()` 和结构化 trace,`Cursor / Claude / Lovable / Manus` 真实验证已能区分"抓取到了页面但提取失败""正确空结果""无可靠官方目标";`python -m unittest discover -s tests -v` 24 项通过,`python -m compileall .\\app .\\main.py .\\tests` 通过,默认 API 响应结构未被污染,可合并到主分支

✅ [2.S4] GitHub 多字段搜索扩展 + 置信度阈值调低 + Bing fallback：当 GitHub repo 的 homepage 字段为空时，用 repo 全名 Bing 搜索官网补充；`_is_confident_github_candidate` 阈值从 120 降至 80；`_search_github_repository` 扩展搜索范围至 `in:name,description,topics` 三字段 | 完成于 2026-04-02 | 测试结论:22/24 项单元测试通过;2 个 ERROR 为既有 `entry_extractor.py:_looks_like_qq_group` 空指针问题,与本次改动无关;符合 PRD §3(GitHub 为官方源之一)的定位,GitHub 搜索扩范围不改变产品边界,代码审查通过,可合并到主分支

✅ [2.S5] `collect_relevant_internal_links` 中文关键词扩展:新增 `微信群`、`QQ群`、`交流`、`二维码`、`扫码`、`入群`、`官方群`、`内测`、`招募` 共 9 个关键词 | 完成于 2026-04-02 | 测试结论:`python -m unittest discover -s tests -v` 22/24 项通过,既有 ERROR 不受本次改动影响;关键词扩展不改变任何判断逻辑,仅提升中文官网内链发现召回率,可合并到主分支

✅ [2.S6] QR 码识别增强:修复 `_looks_like_qq_group` 和 `_detect_platform` 对 `source=None` 的空指针防护（导致 2 个单元测试 ERROR）；`_extract_image_candidate` 修复 QR 检测到但解码失败时（`is_qrcode=True, decoded_payload=None`）降级使用 `image_url` 做平台检测；`_decode_qrcode` 实现多策略解码（原始图 → 灰度 → CLAHE 对比度增强 → Otsu 二值化 → 小图上采样），共 5 种预处理策略轮询 | 完成于 2026-04-02 | 测试结论:`python -m unittest discover -s tests -v` 24/24 项全部通过;`python -m compileall` 无错误;符合 PRD §4"中等严格"识别规则,可合并到主分支

✅ [2.S7] Playwright 懒加载图片等待增强:在 `<details>` 展开后增加 500ms DOM settle 等待;增加 `data-src` 懒加载图片的显式等待逻辑,主动将 `data-src` 内容 swap 到 `src` 后等待 300ms,确保 QR 码图片在 HTML 被捕获前已加载 | 完成于 2026-04-02 | 测试结论:`python -m unittest discover -s tests -v` 24/24 项全部通过;不改变任何业务逻辑,仅改善 Playwright 动态渲染的页面捕获质量,可合并到主分支

✅ [前端-F1] 重复群子卡片去重:当同一产品返回多个相同的 group 子卡片时(同一 platform×groupType 组合),前端按 platform+groupType 去重,每个组合只保留一个子卡片;同时统一入口按钮只渲染一个 | 完成于 2026-04-02 | 测试结论:`npm run build` 通过;纯前端展示层改动,不动后端接口和业务逻辑,可合并到主分支

---

# 四、已知问题记录

> 格式:`❌ [日期] [问题描述] | 影响范围:[模块] | 状态:待修复 / 已修复 | 修复人:xxx`

❌ [2026-04-02] 输入完整 GitHub URL(如 `https://github.com/labring/FastGPT`)被 `DOMAIN_PATTERN` 错误解析为 domain=`github.com`,导致系统抓取 GitHub 首页而非目标仓库页 | 影响范围:[SearchEntry / SearchService] | 状态:已修复 | 修复人:Backend Agent

❌ [2026-04-02] 二维码提取链路保守：只识别静态 `<img src|data-src|srcset>` + OpenCV 解码，折叠区 QR 码、上下文措辞不标准、URL 含平台关键词但无法解码等情况均直接丢弃 | 影响范围：[EntryExtractor / _extract_image_candidate] | 状态：已修复 | 修复人：Backend Agent

❌ [2026-04-02] 关键词搜索发现层单点依赖 DuckDuckGo HTML 结果页;该页面当前会返回反爬挑战页(HTTP 202),导致 `official_results` 长期为空,产品名搜索几乎总是直接走空结果态 | 影响范围:[SearchService / 关键词发现层] | 状态:已修复 | 修复人:Codex

❌ [2026-04-02] 搜索链路缺少阶段性可观测性,无法判断结果被吃在"目标发现 / 页面抓取 / 入口提取"的哪一步 | 影响范围:[SearchService / PageFetcher / EntryExtractor] | 状态:已修复 | 修复人:Codex

❌ [2026-04-02] 部分真实搜索(如 `cursor.com`)端到端耗时约 12.9s,暂未达到 `< 10s` 性能目标 | 影响范围:[SearchService / PageFetcher / Phase 4.10] | 状态:待修复 | 修复人:Codex

❌ [2026-04-02] `Claude` 文档页命中偏差与 `Manus` 歧义 repo 误命中会把错误主体当成官方目标 | 影响范围:[SearchService / 官方目标发现] | 状态:已修复 | 修复人:Codex

❌ [2026-04-02] 真实关键词搜索仍存在"目标正确但入口提取为 0"的剩余问题;当前瓶颈已收敛到提取规则、上下文噪音与页面实际无微信/QQ/飞书官方群入口的区分 | 影响范围:[EntryExtractor / GroupTypeClassifier / Phase 4.1~4.5] | 状态:待修复 | 修复人:待分配

❌ [2026-04-02] `entry_extractor.py:_looks_like_qq_group` 未对 `source=None` 做空指针防护：当 QR 解码返回 `(None, True)` 时，`_detect_platform` 将 `None` 传入 `_looks_like_qq_group` 导致 `AttributeError` | 影响范围:[EntryExtractor / QR 提取] | 状态:已修复(2.S6) | 修复人:Claude

🟡 [2026-04-02] 本轮搜索准确率改进(2.S4~2.S7):GitHub 多字段搜索、阈值调低、Bing fallback、关键词扩展、QR 多策略解码、懒加载等待增强;预期 `Minimax` 等产品名搜索可返回产品卡片(即使暂无二维码);二维码提取成功率仍依赖官网页面结构,非本轮改进范围 | 影响范围:[SearchService / PageFetcher / EntryExtractor] | 状态:本轮改进完成,待真实查询验证 | 修复人:Claude

处置说明:
- 已做本地复核:`Cursor`、`Claude`、`Lovable`、`Manus` 四个真实查询仍返回 0,但 trace 已能明确区分原因。
- `Cursor`:当前命中 `https://cursor.com` 与 GitHub 官方仓库,抓到 7 个页面,`EntryExtractor` 扫描 1175 个标签后仍为 0;归类为**抓取到了页面但提取失败**,下一轮应继续收缩上下文噪音并提升平台/二维码判定。
- `Claude`:官方目标已从文档页修正为 `https://claude.ai/`,但当前抓取实际落到登录页 `https://claude.ai/login`,配合 GitHub 页面后仍提取为 0;归类为**抓取到了页面但提取失败**,问题已不再是文档页误命中。
- `Lovable`:当前命中 `https://lovable.dev/` 及其 community / support 页面,页面主入口仍是 Discord;按当前平台边界(只收录微信 / QQ / 飞书官方群),归类为**正确空结果**。
- `Manus`:当前不再误命中 `OpenManus`,官方网页候选与 GitHub candidate 都未达到置信门槛;归类为**无可靠官方目标,按宁缺毋滥返回空结果**。

后续处理顺序(必须按优先级执行):
1. **本轮优先提升目标发现召回率(2.S4 + 2.S5),再修提取细节**
   - 策略调整:之前"继续修提取阶段、不回头盲调目标发现"已不适用;本轮已在目标发现层(GitHub 多字段搜索 + 阈值降低 + Bing fallback)做了直接改进。
   - `_search_github_repository` 现在跨 name / description / topics 三字段搜索;`_is_confident_github_candidate` 阈值降至 80;`_discover_targets` 在 GitHub candidate 存在但 homepage 为空时 Bing fallback。
   - 下一轮若仍需改进目标发现,优先评估:降低 `_select_official_site` 90 分阈值至 70(配合 PRD"宁缺毋滥"原则,仅作为 low-confidence fallback)。
2. **继续沿着 trace 修提取阶段**
   - 当前 `search_with_trace()` 已输出:目标发现结果、抓到的页面列表、命中的内链列表、提取扫描量与过滤计数。
   - 后续所有规则调整都应基于 trace 判断,而不是只看最终是否返回空结果。
3. **建立正反例样本集,停止用不适合作为正例的产品验证二维码链路**
   - 正例:官网 / GitHub 官方页上**确定存在微信 / QQ / 飞书入口**的产品。
   - 反例:只有 Discord / Telegram / 联系我们 / 社区页但无官方群入口的产品。
   - 干扰例:品牌歧义明显、容易误命中的产品(如 `Manus`)。
   - Phase 4 的端到端测试样本在修复前必须先更新,否则会持续把"正确空结果"误记为 bug。
4. **再做提取链路的最小必要修补**
   - `collect_relevant_internal_links()` 抓取上限从 5 提高到 12~20,并扩展链接关键词(`join`、`invite`、`wechat`、`qr`、`support`、`forum` 等)。
   - Playwright 不直接一刀切改为 `networkidle`;优先采用"`domcontentloaded` + 短暂等待 / 短超时 `networkidle` 回退"的组合,避免因常驻轮询导致整体搜索时间继续恶化。
   - 修复 QQ 平台识别过于依赖空格边界的问题;当前写法会漏掉 `QQ群`、`QQ官方群`、`加入QQ群` 等常见文案。
   - 扩展图片提取范围:除 `img[src]` 外,继续覆盖 `data-src`、`srcset`、父节点中的包裹链接等常见结构。
5. **最后再评估外部搜索兜底**
   - 若在现有 Bing RSS + GitHub 搜索下仍无法稳定找到官方站点,再评估接入 Google Search API / SerpAPI。
   - 在 P0 可观测性和目标发现精度未补齐前,不应优先引入新的付费或外部依赖。

结论:
- 当前"搜索搜不到官方二维码"已不能再归因于"缺少可观测性"或"明显目标误命中"这两类 P0 问题。
- 更准确的说法是:**当前剩余 0 结果主要分成三类:目标正确但入口提取失败、按平台边界应返回正确空结果、以及在品牌歧义下无可靠官方目标。**

---

# 五、关键设计决策记录

> 格式:`📌 [日期] [决策内容] | 决策理由 | 影响范围`

| 日期 | 决策内容 | 决策理由 | 影响范围 |
|------|---------|---------|---------|
| 2026-04-01 | 代码统一放入 `app/` 子目录,文档目录保持原位 | 当前仓库是 Obsidian 文档仓,需要在不扰动 `开发日志`、`产品上下文`、`OpenClaw` 的前提下新增实际代码 | 仓库结构、README、后续所有代码路径 |
| 2026-04-02 | 正式搜索只在 Enter / 搜索按钮 / 候选选择后触发;输入阶段最多做候选联想 | 避免"输入即抓取"与"显式提交搜索"两套交互口径并存 | SearchBox、CandidateDropdown、前后端搜索触发逻辑、文档说明 |
| 2026-04-02 | 弱更新失败时不回退旧结果,直接返回空结果态 | 保持"每次搜索实时抓取,不用旧结果冒充最新结果"的统一口径 | ARCHITECTURE、README、前后端异常处理 |
| 2026-04-02 | 本轮前端只实现"搜索输入 → 触发搜索 → 结果列表/空结果态"闭环,不为候选联想、筛选、已添加做占位交互 | 当前目标是先验证最小可用链路,避免未接后端能力的 UI 占位误导测试口径 | App.tsx、README、开发日志 |
| 2026-04-02 | 关键词发现层改为"GitHub 仓库搜索优先 + Bing RSS 回退",不再单点依赖 DuckDuckGo HTML | DuckDuckGo 当前会返回反爬挑战页;Bing 普通 HTML 也不稳定,但 RSS 结果可稳定解析,结合 GitHub `homepage` 更容易回到官方站点 | SearchService、关键词搜索命中率、后续 CandidateResolver 设计 |
| 2026-04-02 | 官方群入口发现继续采用"**官方网页 HTML / DOM 提取为主,GitHub API / GitHub 搜索为辅**"的路线,不改成"直接在 GitHub 上搜索群聊链接" | GitHub API 更适合做官方仓库 / 官网主页 / 品牌主体发现,不适合作为官方群入口主提取源;真实官方群二维码更常见于官网、community 页面、帮助中心、文档站或官方文章页。若把 GitHub 直接当主要群入口搜索源,既会漏掉官网二维码,也会放大仓库镜像、社区仓库、教程仓库的误命中风险 | SearchService、官方目标发现策略、EntryExtractor、样本集建设、后续外部搜索接入决策 |
| 2026-04-02 | 搜索问题的修复顺序定为"**可观测性 → 官方目标发现 → 页面抓取 → 入口提取 → 外部搜索兜底**",禁止直接通过放宽提取规则硬凑结果 | 当前 0 结果的根因混合了"正确空结果"和"错误目标命中";如果在没有中间日志和样本集的情况下直接放宽二维码/链接规则,会把 Discord、联系我们、非官方群和误命中仓库混进结果里,破坏产品"宁缺毋滥"的边界 | Phase 4.1~4.11、后端 debug 设计、测试样本集、后续 reviewer 判断标准 |
| 2026-04-02 | 端到端验证样本必须分为"正例 / 反例 / 干扰例",停止单纯用 `Cursor / Claude / Lovable / Manus` 作为唯一主样本 | 这四类查询同时混合了"品牌歧义""仅 Discord 社区""文档页误命中""可能无微信/QQ/飞书官方群"等情况,无法单独用来判断二维码链路是否失效;只有先分离样本,后端和前端编程助手才能知道是在修 bug 还是在处理正确空结果 | Phase 4.1、4.4、4.5、4.6、样本库建设、后续自动化测试 |
| 2026-04-02 | 默认 API 响应不扩展 debug 字段;调试统一走 `search_with_trace()` 内部结构和可选结构化日志 | 普通接口响应必须保持稳定,不能为了调试污染前端契约;同时后端必须能回答"结果在哪一步被吃掉" | SearchService、后端日志、测试脚本、后续稳定化排障流程 |
| 2026-04-02 | GitHub 完整 URL 输入直接作为 `github_repo_url` 处理,不走 domain 解析路径 | 用户输入 `https://github.com/{owner}/{repo}` 时不应被当作"官网域名"解析;通过在 `SearchEntry.normalize()` 中优先匹配 `GITHUB_URL_PATTERN` 并设置 `query_type=github_repo`,由 `_discover_targets()` 专用分支直接路由 | SearchEntry、SearchService、GitHub URL 直接输入场景 |
| 2026-04-02 | QR 码解码失败时降级策略：URL 含平台关键词（`feishu.cn` / `qm.qq.com` / `weixin.qq.com` 等）时不丢弃候选，降级为 link 型入口 | 当 QR 码因图片压缩/裁切/OpenCV 识别率问题无法解码时，图片 URL 本身可能已经包含平台标识；此时不依赖解码结果，直接用 URL 做平台判断并返回 link 型入口，附带"二维码暂未抓取成功"标注 | `_extract_image_candidate`、ResultNormalizer、前端卡片展示 |
| 2026-04-02 | Playwright 页面抓取时主动展开 `<details>` 折叠区域 | GitHub README 及大量官网的 QR 码藏在折叠区，初始 HTML 的 DOM 里不存在；通过 `page.evaluate()` 主动为所有 `<details>` 设置 `open` 属性后再提取 HTML，确保折叠内容被纳入扫描范围 | `PageFetcher._fetch_with_playwright`、QR 提取召回率 |
| 2026-04-02 | GitHub API 元数据(`/repos/{owner}/{repo}`)中 `homepage` 字段作为官网 URL 发现的重要补充来源 | 当用户输入 GitHub URL 时,用 `homepage` 字段(而非 GitHub 仓库描述)填充 `official_site_url`,确保仍然能抓取对应官网的内链和二维码 | SearchService、GitHub API、元数据字段、`_discover_targets` |

---

# 六、版本发布记录

| 版本 | 日期 | 发布内容 | 状态 |
|------|------|---------|------|
| - | - | - | - |

---

# 七、大模型开发指南

> ⚠️ **每次开始开发前必读**

## 快速上手(3 分钟)

1. **读取本文件(§ 二 + § 三)**,了解哪些任务已完成、哪些待做
2. **读取[[产品总纲]]**,确认产品口径和术语表
3. **读取对应模块的文档**(PRD / ARCHITECTURE / DESIGN)
4. **从 TODO LIST 最前面的未完成任务开始**,按 Phase 顺序开发

## 上下文恢复(vibe coding 中断后)

如果开发中断,再次开始时请按以下步骤恢复上下文:

```
Step 1: 读取本文件 § 二
         → 确认当前 Phase 和具体任务编号

Step 2: 读取本文件 § 三
         → 确认最近完成的任务编号和测试结论

Step 3: 读取本文件 § 四
         → 确认已知问题,避免重复踩坑

Step 4: 读取对应模块的最新代码文件
         → 确认代码当前状态

Step 5: 继续开发
```

## 开发原则

| 原则 | 说明 |
|------|------|
| **不跳 Phase** | Phase 1 未完成不得开发 Phase 2 |
| **测试后再标记** | 每个任务完成后立即在 § 三记录测试结论 |
| **不改产品口径** | 发现产品总纲未覆盖的需求,报告主 Agent,不自行决定 |
| **不引入 MVP 外的功能** | 不做登录、团队共享、钉钉/Telegram/Discord |
| **术语严格对齐** | 使用产品总纲术语表中的标准词(见总纲 §2) |

## 技术约束提醒

- 后端:FastAPI + httpx + BeautifulSoup4 + Playwright + OpenCV + SQLite
- 前端:React + Vite + TypeScript,无 UI 框架依赖(MVP 保持轻量)
- 不引入付费 API / 远程数据库 / 用户鉴权服务
- 二维码识别用 OpenCV 本地实现,不调用云端 OCR

---

> 📌 **提醒**:完成每个任务后,记得回到本文件更新:
> 1. § 二 TODO LIST:标记 `[x]` + 完成日期 + 测试结论
> 2. § 三 已完成记录:追加完成条目
> 3. § 四 已知问题:如有问题及时记录
> 4. § 五 关键设计决策:如做了非文档约定的技术决策
