## 目前的搜索策略

![[Pasted image 20260410095251.png]]若搜索某关键词，设置返回结果为50，可能会发生：
1. GitHub 搜索可能只找到 **8 个候选仓库**
2. 候选扩展补了 **3 个**，Web 兜底又补了 **5 个** → 共 **16 个候选**
3. 逐个爬取这 16 个候选的页面，提取群入口
4. 其中可能只有 **4 个**候选的页面里真的有群聊链接
5. → 最终返回 **4 个结果**，附带空消息提示：`"未在 GitHub/官网相关页面中发现官方群入口"`
6. 不会因为"不够 50"而去凑不相关的结果
## GPT优化前后对比
| 阶段  | “code” 结果数 | “code” 耗时 | “agent” 结果数 | “agent” 耗时 |  时间差 |  提速% | 单条耗时提升% |
| --- | ---------: | --------: | ----------: | ---------: | ---: | ---: | ------: |
| 1   |         10 |     2m00s |          10 |      1m14s | -46s | 38.3 |    38.3 |
| 2   |         15 |     3m15s |          17 |      3m00s | -15s |  7.7 |    18.5 |
| 3   |         21 |     4m10s |          22 |      3m57s | -13s |  5.2 |     9.2 |
| 4   |         30 |     5m10s |          33 |      5m05s |  -5s |  1.6 |    10.7 |

>   说明：
> - **提速%** = (code耗时 − agent耗时) / code耗时 × 100
> - **单条耗时提升%** = (code秒/条 − agent秒/条) / code秒/条 × 100
>   - code 秒/条：12.0 → 13.0 → 11.9 → 10.3
>   - agent 秒/条：7.4 → 10.6 → 10.8 → 9.24
>  
> - agent 在每个阶段都返回了**相同或更多**的结果，且耗时更短
> - 初始阶段（前10条）提速最显著（38%），符合渐进式加载的设计预期；随着结果数增加，两者趋于接近

## ~~可以继续优化的内容~~
- ~~P0：`_filter_viewed_cards` 在后台任务中反复查库~~
	~~`_filter_viewed_cards` 内部每次都调用 `_load_viewed_group_filters()`，**每次都打开 SQLite 连接、全表扫描 `viewed_groups`**。如果有 12 个 batch，就是 12 次 DB 查询。~~
	~~**建议**：在 `_run_search_job` 开头加载一次 viewed 过滤器，后续 batch 复用：~~
- ~~P0：种子页 extraction 做了两遍~~
	~~**种子页的 extraction 被执行了两次**，而 extraction 涉及图片下载+QR解码，是最耗时的操作之一。~~
	~~**建议**：让 `_fetch_candidate_pages` 返回已提取的种子结果，或者在 `_collect_cards` 层面做"先试种子页，有群就不扩展"的决策。~~
- ~~P1：旧 `search()` 方法成了死代码~~
	~~`routes.py` 已经改为调用 `search_with_job()`，但 `service.py` 中旧的 `search()` 方法（第 381–477 行）仍完整保留。3500 行的文件里多了 100 行无用代码。~~
	~~**建议**：删除旧 `search()` 或标记为 `@deprecated`。~~
- ~~P1：`routes.py` 开头多了 BOM~~
	~~文件开头被 GPT 加了一个 UTF-8 BOM (`\ufeff`)。这在某些环境下可能导致 import 异常。~~
- ~~P1：`page_cache_lock` 类型标注不正确~~
	~~实际传入的是 `Lock()`，但类型标注为 `object`。应改为 `Lock | None`。~~
- ~~P2：`_prepare_search` 对小 limit 做了多余工作~~
	~~即使用户只要 5 条，`_prepare_search` 仍然按 50 的规模搜 GitHub + 构建候选。这是为了填充缓存，但首次响应的 latency 会被拖慢。~~
	~~**建议**：考虑首次同步阶段只搜 `limit` 数量的候选，后台任务再补到 50。~~
## GPT优化前后对比

~~搜索“claw”：~~
- ~~10：1min02s~~
- ~~**15：4min**~~
~~**新引入的问题：`_prepare_search` 被调用了两遍**~~

搜索“chat”：
- 10：1min
- 17：2min41s
- 21：3min10s
- 30：4min17s

| 阶段  | “agent” 结果数 | “agent” 耗时 | “chat” 结果数 | “chat” 耗时 |  时间差 |  提速% | 单条耗时提升% |
| --- | ----------: | ---------: | ---------: | --------: | ---: | ---: | ------: |
| 1   |          10 |      1m14s |         10 |     1m00s | -14s | 18.9 |         |
| 2   |          17 |      3m00s |         17 |     2m41s | -19s | 10.5 |         |
| 3   |          22 |      3m57s |         21 |     3m10s | -47s | 19.8 |         |
| 4   |          33 |      5m05s |         30 |     4m17s | -48s | 15.7 |         |

---
## 已知问题
- 目前搜索部分关键词，如Minimax，会出现从官网返回的结果，该结果是只从官网抓取，所以Github星级为0。但大部分关键词似乎都是github、官网合并为一张卡片。
	- minimax的案例为什么没有合并为一张卡片？
	- 如何更统一的管理？如增加筛选官网和github来源？这样github星级筛选只控制从github上抓取的群聊。这样做可行吗？有没有其他管理方案？