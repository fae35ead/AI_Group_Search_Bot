## 2026-04-03 12:00 已知问题
- 目前基于关键词搜索返回的结果有几点需要改进：
	- 返回的群二维码没有显示出来。
	![[Pasted image 20260403091835.png]]
	- 抓取的群二维码有时返回的是别的图片，如聊天记录等，详情可见backend/data文件夹。所以需要在目前的抓取图片的基础上，进行简单的二维码识别，不要使用过于重量级的方法如OpenCV识别。可能需要正则匹配？或是其他轻量级的方法确保返回的结果为群聊二维码。
	- 若未发现官方群，则修改为无需显示结果。

## 2026-04-03 处理结果
- 本轮以 `PRD.md` 为准，支持平台统一为：微信、QQ、飞书、Discord。
- 已修复“二维码没有显示”的主因：
	- 后端搜索不再只抓 GitHub 仓库主页，恢复补抓 `homepage`、官网根域和有限相关页。
	- 提取器补充了“图片链接型二维码”识别，像 `FastGPT` 官网这种直接把飞书二维码挂在 `.png` 链接上的场景可被捕获。
- 已修复“误抓聊天截图等非二维码图片”的主因：
	- 图片下载前先做平台与群聊语义前置过滤。
	- 图片下载后只做轻量规则校验，过滤明显非方图、过小图片和普通插图。
	- OpenCV 依赖保留，但二维码解码默认关闭，不再作为主识别路径。
- 已按本篇笔记要求调整结果规则：
	- 若未发现微信 / QQ / 飞书 / Discord 官方群入口，则该工具不展示在结果列表中。


## 2026-04-03 14:22 已知问题
- 几乎所有的群聊二维码都没有抓取成功：
![[Pasted image 20260403142329.png]]但可以成功返回含有二维码的网页链接。
- 返回的网页链接中可能含有二维码，但不一定是群聊的二维码。如：
![[Pasted image 20260403144210.png]]
![[Pasted image 20260403142435.png]]如何识别提取正确的群聊二维码而非其他的二维码？周围文字检查？
- 有一些二维码是含在一整个大的图片中的，没有办法单独下载文件并提取出来。如：
![[Pasted image 20260403142607.png]]有什么方法可以既对群聊二维码判断正确，又能提取二维码并返回前端呢？
- 如果二维码是以图片链接的形式放出，会更容易被识别到且正确返回。
- 有些抓取官方网站和Github上的群聊二维码重复。
![[Pasted image 20260403144322.png]]![[Pasted image 20260403144330.png]]
- 目前搜索响应速度偏慢，搜索流程如下：

		用户输入 "FastGPT"
			  ↓
		  GitHub Search API（in:name/description/readme）
			  ↓
		  候选仓库列表（按 stars 排序，最多 10 个）
			  ↓ 对每个候选仓库并行/串行执行：
				  ① 抓取仓库页 + homepage + 最多 2 个相关页（共 5 页）
				  ② 每个页面扫描 <img> 标签 → 下载图片 → OpenCV 解码 → 平台检测
				  ③ 每个页面扫描 <a> 标签 → 平台检测 → 如果是图片链接则同 ②
				  ④ 三层过滤：decode 成功 → detect 成功 → 文本降级
				  ⑤ 去重（同平台+同URL）
			  ↓
		  构建 ProductCard → 保存 QR 图片到 /assets/qrcodes/
			  ↓
		  前端展示：二维码图片 + 跳转链接
		  
		如何判断是二维码 — 三层检测
		
		  L1（最严格）：OpenCV 解码成功 + 平台关键词在内容中
		  decoded_payload, qr_detected = self._decode_qrcode(image_bytes)
		  platform_from_decode = self._detect_platform(decoded_payload) if decoded_payload else None
		  # decode成功说明确实是QR码，且内容含平台关键词
		  if decoded_payload and platform_from_decode:
		      return ExtractedGroupCandidate(...)
		  - OpenCV QRCodeDetector().detectAndDecode() 能解码出内容
		  - 内容里含 wechat/微信/feishu/discord.gg 等
		
		  L2（中等）：OpenCV 检测到QR特征但解码失败
		  if qr_detected:
		      platform_from_text = self._detect_platform(image_url, full_context)
		      # 修复前：qr_detected + platform_from_text 即可
		      # 修复后：qr_detected + platform_from_text + (group_intent OR decoded_payload)
		      if platform_from_text and (self._has_group_intent(full_context.lower()) or decoded_payload):
		          ...
		  - qr_detected：OpenCV 检测到图像中有 QR 码特征（但解码不出来，比如扭曲/损坏/非标准）
		  - 平台判断依赖 URL/上下文文本（<img> alt、父级文字、周围标题）
		
		  L3（最宽松）：无 QR 特征，但平台关键词 + 尺寸足够
		  platform_from_text = self._detect_platform(image_url, full_context)
		  has_min_size = width >= 400 and height >= 400 and len(image_bytes) >= 20_000
		  # 修复前：has_group_intent AND has_min_size
		  # 修复后：has_min_size 即可
		  if platform_from_text and has_min_size:
		      ...
		  - 根本没有任何 QR 特征，仅凭"图片在微信/QQ/飞书/Discord 相关上下文中，且尺寸够大"推断
		  - 这是最不可靠的一层

能否第一次查找到二维码存入本地后第二次直接调用本地的数据？但这样有个问题，在群聊二维码筛选机制做好前，不能这么做，因为可能会一直调用非群聊二维码的数据。
- 目前的搜索机制似乎还未完善：如在推荐列表中的n8n，在官网中含有discord的小图标，为跳转链接https://discord.gg/n8n。但产品搜索不到。

## 2026-04-03 目前新问题
- discord的链接是否过滤太宽松，导致许多并非群聊邀请链接也被纳入其中。不应该只靠一个词就命中。如大多数负例：[Discord](https://discord.com/invite/sTVN9EVS4B)。同时还有discord官网链接、telegram链接也被识别为discord链接。
	- 问题回答：`fallback_url` 优先取 `entry_url`（最近链接），而不是 decoded_payload 中的真实群 URL。所以"打开官方群入口"可能跳到了一个需要扫码的页面，而不是扫码后直接进群。
	- 2026-04-03 17:20 在claude code加强discord限制后，搜索coze返回的内容和coze完全无关。不知道什么内容被修改了。
- 
	- 后端只检查 `official_groups` 非空，不检查是否有图？是后端的原因？
- 目前的识别准确率还是偏低，如github readme里有二维码，但是返回了多个无关的链接。如返回”[kone-net/mcp_server_lark: A Model Context Protocol server for Lark(Feishu) sheet, message, doc and etc.](https://github.com/kone-net/mcp_server_lark)“但实际上是因为可能该链接里有feishu的字眼，但是这不是一个飞书群聊链接，而只是一个关于飞书的网址链接。
- 是否应该引入“混合策略”：**OpenCV 负责检测/裁剪，ZBar 负责二次解码兜底**？因为“ZBar（通常通过 pyzbar）在“**二维码解码成功率**”上，很多场景会比 OpenCV 更稳，尤其是压缩图、轻度模糊图。”
- “换一批”按钮的逻辑应该是更换新的关键词，而非只推荐热度高的关键词。
- 目前返回结果太少，放宽抓取限制的条件：
	- 有一些页面如open-im-server在github上的群聊并不是二维码形式，即使是微信群，也给的是链接：https://openim-1253691595.cos.ap-nanjing.myqcloud.com/WechatIMG20.jpeg，需要识别出这是微信群聊的链接。
	- 搜索关键词返回结果太少，如搜索openclaw，只返回了一个结果，但我在github上能快速返回许多个相关结果。其中含有Clawith这样github页面中有群聊链接，但没有被返回的卡片。我们只能通过自行搜索Clawith才能返回对应卡片。

## 2026-04-03 未来的新功能
- 加入“入库“功能：添加手动按钮标记已入库，入库后下一次搜索就不再展示。
- 今日推荐展示出的候选关键词可以再丰富一些，除了最热门的AI工具，还可以展示最近比较流行（抓取最近趋势）的等等。
- 添加钉钉群的搜索。