# 代码审查报告

> **审查时间：** 2026-04-04（更新）
> **审查范围：** 后端核心逻辑、前端 API 层、配置文件
> **更新说明：** 本次更新 2026-04-04，标记了自上次审查以来的修复项，并新增了本轮发现的问题。

---

## 🔴 高优先级问题

### 1. 安全问题

#### 1.1 CORS 配置过于宽松 ✅ 已修复
**位置：** `main.py`
**修复：** `allow_origins` 改为 `settings.cors_origins`，值为 `('http://127.0.0.1:5173', 'http://localhost:5173')`

#### 1.2 GitHub Token 硬编码风险 ⚠️ 部分缓解
**位置：** `config.py`
**现状：** Token 通过 `.env` 管理，未硬编码。但缺少 `.env.example` 模板文件。

---

### 2. 错误处理缺失

#### 2.1 数据库连接失败无重试 ⚠️ 未修复
**位置：** `database.py`

#### 2.2 GitHub API 限流处理不完整 ✅ 已修复
**位置：** `service.py`
**修复：** 限流时抛出 `httpx.HTTPStatusError`，不再静默返回空结果。

---

### 3. 性能问题

#### 3.1 Playwright 浏览器未复用 ✅ 不适用
**说明：** 项目已移除 Playwright，不存在此问题。

#### 3.2 数据库查询未优化 ⚠️ 未修复
**位置：** `service.py`

---

## 🟡 中优先级问题

### 4. 代码质量

#### 4.1 类型注解不完整 ⚠️ 部分修复
**现状：** 核心方法已有类型注解，建议后续启用 `mypy --strict`。

#### 4.2 魔法数字 ✅ 已修复
**修复：** `per_page` 硬编码移除，改为 `_github_search()` 内的动态 `min(25, limit * 2)` 等表达式。

#### 4.3 日志级别不统一 ✅ 基本修复
**现状：** 日志统一使用 `logger`；`print` 已清除。

---

### 5. 测试覆盖

#### 5.1 缺少单元测试 ✅ 已修复
**现状：** 12 个单元测试，全部通过。

#### 5.2 缺少集成测试 ❌ 未修复

---

### 6. 文档与规范

#### 6.1 缺少 API 文档 ⚠️ 未修复
**现状：** FastAPI 自带 `/docs`，但缺少自定义使用说明。

#### 6.2 缺少部署文档 ❌ 未修复

#### 6.3 环境变量未文档化 ✅ 已修复
**现状：** `app/frontend/.env.example` 已创建。

---

### 7. 前端问题

#### 7.1 错误边界缺失 ❌ 未修复
**位置：** `App.tsx`

#### 7.2 加载状态不明确 ✅ 已修复
**现状：** Skeleton loading 已有实现。

#### 7.3 图片加载失败无降级 ❌ 未修复
**建议：** 给 `<img>` 添加 `onError` fallback 处理。

---

## 🟢 低优先级问题

### 8. 资源泄漏风险

#### 8.1 图片下载无超时和大小限制 ✅ 已修复
**位置：** `entry_extractor.py - _download_image()`
**修复：** data URL 限制 5MB，HTTP 响应限制 5MB。

#### 8.2 BeautifulSoup 解析无限制 ❌ 未修复
**位置：** `entry_extractor.py - extract()`

---

### 9. 逻辑缺陷

#### 9.1 图片宽高比判断 ✅ 已修复
**修复：** `_extract_from_img_tag` 用 1.35，`_extract_from_link_tag` 和 `_passes_visual_checks` 用 1.5。

#### 9.2 候选数量限制 ✅ 已修复
**现状：** `MAX_CANDIDATES_PER_PAGE = 10` 作为模块常量，可配置。

---

### 10. API 设计问题

#### 10.1 缺少分页支持 ❌ 未修复

#### 10.2 缺少请求限流 ✅ 已修复
**修复：** `slowapi` 限流 10次/分钟，已集成到 `routes.py` 和 `main.py`。

#### 10.3 错误响应格式不统一 ❌ 未修复

---

### 11. 前端依赖和配置问题

#### 11.1 缺少状态管理 ❌ 未修复

#### 11.2 缺少 HTTP 客户端库 ❌ 未修复

#### 11.3 缺少环境变量配置 ✅ 已修复

---

### 12. 安全加固建议

#### 12.1 缺少输入验证（SSRF） ❌ 未修复
**位置：** `entry_extractor.py`
**建议：** 添加 URL 白名单，仅允许 `github.com`、`raw.githubusercontent.com` 等已知域名。

#### 12.2 SQL 注入防护 ✅ 不适用
**现状：** 使用 ORM，无原始 SQL 拼接。

#### 12.3 敏感信息可能泄露 ❌ 未修复

---

## 🎯 优先修复建议

### 已完成（本次迭代）

| 优先级 | 问题 | 修复状态 |
|--------|------|---------|
| P0 | CORS 安全风险 | ✅ 已修复 |
| P0 | 图片下载大小限制（资源泄漏） | ✅ 已修复 |
| P0 | 图片宽高比过严（1.35） | ✅ 已修复 |
| P1 | 图片预过滤逻辑 bug | ✅ 已修复 |
| P1 | 搜索性能（超时过长、连接不复用、Bing RSS 串行） | ✅ 已修复 |
| P1 | 请求限流 | ✅ 已修复 |
| P1 | 前端过滤改客户端行为（筛选即时响应） | ✅ 已实施 |
| P1 | 验证按钮从搜索流程剥离 | ✅ 已实施 |
| P2 | 前端环境变量 `.env.example` | ✅ 已创建 |

### 待修复（后续迭代）

| 优先级 | 问题 | 建议 |
|--------|------|------|
| P1 | 前端 Error Boundary | 添加 React Error Boundary |
| P1 | 图片 `onError` fallback | 给 `<img>` 加降级处理 |
| P1 | BeautifulSoup HTML 解析无限制 | 限制 HTML 最大长度 |
| P2 | 统一错误响应格式 | 定义 `ErrorResponse` model |
| P2 | SSRF URL 白名单 | 仅允许已知域名列表 |
| P2 | 数据库连接重试 | 指数退避重试 |
| P2 | 前端状态管理 | 考虑 Zustand |
| P3 | API 分页 | `page`/`page_size` 参数 |
| P3 | 部署文档 | Docker Compose |
| P3 | 日志脱敏 | 敏感信息打码 |
| P3 | mypy --strict | 严格类型检查 |

---

## ✅ 优点

1. **架构清晰**：前后端分离，职责明确
2. **类型安全**：使用 TypeScript 和 Pydantic
3. **配置灵活**：支持环境变量配置
4. **日志完善**：关键操作有日志记录
5. **测试覆盖**：12 个单元测试覆盖核心逻辑
6. **性能意识**：连接复用、超时控制、预过滤优化
